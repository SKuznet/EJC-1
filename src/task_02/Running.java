package task_02;

public class Running implements MovementBehavior {
    @Override
    public int move() {
        final int MAX_SPEED = 80;
        final int MIN_SPEED = 10;
        return 0;
    }
}
