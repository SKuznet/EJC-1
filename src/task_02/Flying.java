package task_02;

public class Flying implements MovementBehavior {
    @Override
    public int move() {
        final int MAX_SPEED = 100;
        final int MIN_SPEED = 40;
        return 0;
    }
}
